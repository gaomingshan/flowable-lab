## 第一迭代最小表结构设计 1.1 修订

### 1. 修订范围

本修订仅覆盖第一迭代最小表结构的以下变更：

- 新增节点边逻辑骨架表
- 调整候选人模型表结构
- 补充实例快照字段

### 2. 修订内容

#### 2.1 节点边骨架表修订

修订口径：

- 增加 `wf_node`
- 增加 `wf_edge`

#### 2.2 候选人表结构修订

修订口径：

- 保留 `wf_node_candidate_config`
- 不再拆 `wf_node_candidate_user`
- 在 `wf_node_candidate_config` 中增加 `policy_payload_json`

#### 2.3 实例快照字段修订

修订口径：

- `wf_instance` 增加：
  - `starter_code`
  - `starter_dept_id`
  - `starter_dept_code`
  - `starter_dept_name`
  - `starter_org_path`

- 基础读模型同步补充：
  - `starter_dept_id`
  - `starter_dept_name`
